public class GitAmend {
    /*Git amend is used to modify the most recent commit
    * It is recommended to amend only those files that
    * are not pushed else you will need git push --force
    * Supose you missed to commit a file and then  you
    * can fist add then use git amend*/

    /*Added new comment to test amend command*/

    /*Lets test if we can amend after pushing the changes push+amend*/

    /*Lets test using the interface after pushing the changes.*/




}
